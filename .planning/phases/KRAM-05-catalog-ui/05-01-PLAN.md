---
phase: 05-catalog-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/.env.example
  - apps/web/src/main.tsx
  - apps/web/src/app/AppShell.tsx
  - apps/web/src/app/AppLayout.tsx
  - apps/web/src/app/providers.tsx
  - apps/web/src/app/router.tsx
  - apps/web/src/shared/api/client.ts
  - apps/web/src/shared/api/products.ts
  - apps/web/src/shared/lib/formatPrice.ts
  - apps/web/src/features/catalog/ProductListPage.tsx
  - apps/web/src/features/catalog/ProductDetailPage.tsx
  - apps/web/src/features/catalog/NotFoundPage.tsx
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03
user_setup:
  - Copy apps/web/.env.example to apps/web/.env if missing

must_haves:
  truths:
    - "D-05: react-router-dom and @tanstack/react-query installed; QueryClientProvider mounted"
    - "D-06: routes /products, /products/:id, / redirect, 404 catch-all"
    - "D-07: features/catalog pages stubbed; shared/api client and products fetch helpers exist"
    - "D-08: VITE_API_URL in .env.example default http://localhost:3000"
    - "D-18: React Query Devtools only when import.meta.env.DEV"
    - "D-19: README or app/README notes Router vs RouterModule, useQuery vs HttpClient"
    - "App builds and dev server renders layout with outlet"
  artifacts:
    - path: apps/web/src/app/router.tsx
      provides: Route table
      contains: "/products"
    - path: apps/web/src/shared/api/client.ts
      provides: fetchJson base
      contains: "VITE_API_URL"
  key_links:
    - from: apps/web/src/shared/api/products.ts
      to: http://localhost:3000/products
      via: fetchJson GET
      pattern: "/products"

threat_model:
  threats:
    - id: T-05-01
      stride: Tampering
      description: Misconfigured API base URL points browser to attacker host
      asvs: V13.2.1
      mitigation: VITE_API_URL from env with localhost default; document in .env.example only
      severity: medium
---

## Phase Goal

**As a** learner, **I want** the web app shell with routing and TanStack Query wired to the API client, **so that** catalog pages can fetch products in plans 05-02 and 05-03.

<objective>
Install React Router v7 and TanStack Query v5, create app layout with header, define routes with stub catalog pages, and implement `shared/api` fetch helpers for products endpoints.

Purpose: Foundation for CAT-01–04; LEARN-01/02/03 shell.
Output: Runnable `pnpm --filter @kramnik/web dev` with navigable routes and empty/stub pages.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/KRAM-05-catalog-ui/05-CONTEXT.md
@.planning/phases/KRAM-05-catalog-ui/05-RESEARCH.md
@.planning/phases/KRAM-05-catalog-ui/05-UI-SPEC.md
@.planning/PROJECT.md
@apps/web/src/main.tsx
@apps/web/src/app/AppShell.tsx
@apps/api/src/products/products.controller.ts
@packages/types/src/product.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add router, query, and env dependencies</name>
  <files>apps/web/package.json, apps/web/.env.example</files>
  <read_first>apps/web/package.json, .planning/research/STACK.md</read_first>
  <action>
    In apps/web add dependencies: react-router-dom@^7, @tanstack/react-query@^5.
    Add devDependency: @tanstack/react-query-devtools@^5.
    Create apps/web/.env.example with VITE_API_URL=http://localhost:3000
  </action>
  <acceptance_criteria>
    - apps/web/package.json lists react-router-dom, @tanstack/react-query, @tanstack/react-query-devtools
    - apps/web/.env.example contains VITE_API_URL=http://localhost:3000
    - pnpm install succeeds from repo root
  </acceptance_criteria>
  <verify>
    <automated>pnpm install</automated>
  </verify>
  <done>Dependencies installed</done>
</task>

<task type="execute">
  <name>Task 2: Providers, layout, and router</name>
  <files>apps/web/src/main.tsx, apps/web/src/app/providers.tsx, apps/web/src/app/AppLayout.tsx, apps/web/src/app/router.tsx, apps/web/src/features/catalog/ProductListPage.tsx, apps/web/src/features/catalog/ProductDetailPage.tsx, apps/web/src/features/catalog/NotFoundPage.tsx</files>
  <read_first>apps/web/src/main.tsx, apps/web/src/app/AppShell.tsx, 05-UI-SPEC.md</read_first>
  <action>
    Replace AppShell-only render with QueryClientProvider + RouterProvider (createBrowserRouter).
    AppLayout: header "Kramnik Shop", main Outlet, Tailwind container padding.
    Routes: / → Navigate to /products; /products → ProductListPage stub; /products/:id → ProductDetailPage stub; * → NotFoundPage.
    Mount ReactQueryDevtools in providers only when import.meta.env.DEV is true.
    Stub pages: minimal heading proving route works.
  </action>
  <acceptance_criteria>
    - main.tsx renders providers + router (no direct AppShell-only tree)
    - router.tsx defines path /products and /products/:id
    - AppLayout contains h1 Kramnik Shop and Outlet
    - pnpm --filter @kramnik/web build exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <done>Shell routes render</done>
</task>

<task type="execute">
  <name>Task 3: Shared API client and price formatter</name>
  <files>apps/web/src/shared/api/client.ts, apps/web/src/shared/api/products.ts, apps/web/src/shared/lib/formatPrice.ts</files>
  <read_first>apps/api/src/products/dto/list-products-query.dto.ts, packages/types/src/product.ts, 05-RESEARCH.md</read_first>
  <action>
    client.ts: read import.meta.env.VITE_API_URL ?? 'http://localhost:3000'; export async function fetchJson(path, init) returning parsed JSON — throw Error with status text on !res.ok.
    products.ts: fetchProducts(params) builds query string from optional category, q, minPrice, maxPrice; fetchProduct(id) GET /products/:id; return types ProductPublic from @kramnik/types.
    formatPrice(price: string): string using Intl USD format for display.
  </action>
  <acceptance_criteria>
    - shared/api/client.ts uses VITE_API_URL
    - shared/api/products.ts exports fetchProducts and fetchProduct
    - formatPrice('19.99') returns string containing $ and 19.99
    - No dangerouslySetInnerHTML in new files
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <done>API helpers ready for useQuery</done>
</task>

<task type="execute">
  <name>Task 4: Learning notes in README</name>
  <files>apps/web/README.md</files>
  <read_first>.planning/PROJECT.md Angular mapping table</read_first>
  <action>
    Create or update apps/web/README.md with short section: React Router vs Angular RouterModule; TanStack useQuery vs HttpClient+async pipe; env VITE_API_URL; how to run against pnpm dev API.
  </action>
  <acceptance_criteria>
    - apps/web/README.md contains the word useQuery and Router
    - Documents VITE_API_URL and default http://localhost:3000
  </acceptance_criteria>
  <verify>
    <automated>rg -l "useQuery" apps/web/README.md</automated>
  </verify>
  <done>D-19 documentation present</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @kramnik/web dev` shows layout and /products route
- [ ] Navigating to / redirects to /products
- [ ] Unknown path shows 404 page
</verification>
