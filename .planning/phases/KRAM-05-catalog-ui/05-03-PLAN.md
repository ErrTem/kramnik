---
phase: 05-catalog-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
files_modified:
  - apps/web/src/features/catalog/ProductDetailPage.tsx
autonomous: true
requirements:
  - CAT-03
  - CAT-04
  - LEARN-01
  - LEARN-02
user_setup: []

must_haves:
  truths:
    - "D-14: detail shows description, large image, USD price, category badge"
    - "D-15: Add to cart button disabled with coming soon label and Phase 7 helper text"
    - "D-16: skeleton loading on detail route"
    - "D-17: inline error with refetch; 404 shows not found message"
    - "D-04: user can navigate from grid card link to detail"
    - "Product description rendered as text only (no HTML injection)"
  artifacts:
    - path: apps/web/src/features/catalog/ProductDetailPage.tsx
      provides: Detail view
      contains: "coming soon"
  key_links:
    - from: ProductDetailPage.tsx
      to: /products/:id
      via: useParams id + useQuery fetchProduct
      pattern: "fetchProduct"

threat_model:
  threats:
    - id: T-05-03
      stride: Information Disclosure
      description: XSS through product description HTML in seed data
      asvs: V5.3.3
      mitigation: Render description as JSX text children only; never dangerouslySetInnerHTML
      severity: medium
---

## Phase Goal

**As a** shopper, **I want** to view a product detail page with loading and error states, **so that** I can read about an item before cart exists (CAT-03, CAT-04).

<objective>
Implement ProductDetailPage with useQuery by id, skeleton/error/404 states, and disabled add-to-cart placeholder per D-15.

Depends on: 05-01 API client; list navigation from 05-02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/KRAM-05-catalog-ui/05-CONTEXT.md
@.planning/phases/KRAM-05-catalog-ui/05-UI-SPEC.md
@apps/web/src/features/catalog/ProductDetailPage.tsx
@apps/web/src/shared/api/products.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: ProductDetailPage with useQuery</name>
  <files>apps/web/src/features/catalog/ProductDetailPage.tsx</files>
  <read_first>apps/web/src/shared/api/products.ts, 05-UI-SPEC.md detail section, products.controller 404 behavior</read_first>
  <action>
    Read id from useParams; useQuery queryKey ['products', id], queryFn () => fetchProduct(id), enabled !!id.
    Loading: skeleton layout (image block + 3 text lines).
    Error: if fetch returns 404 or error message includes not found, show Product not found + Link to /products; else generic error + refetch button.
    Success: md:grid two columns — image (4:3 or large aspect) with same img onError placeholder as card; column with name, badge, formatPrice(price), description paragraph, disabled button aria-disabled=true text Add to cart (coming soon) and helper text about Phase 7 cart.
    Description: render {product.description} as text in p tag only.
    Back link to /products at top.
  </action>
  <acceptance_criteria>
    - ProductDetailPage.tsx contains useQuery and disabled button with coming soon
    - Clicking a product card from list opens detail with name and description visible
    - Invalid id URL shows not found UI
    - pnpm --filter @kramnik/web build exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <done>CAT-03 and CAT-04 detail flow complete</done>
</task>

<task type="execute">
  <name>Task 2: Improve fetch error mapping for 404</name>
  <files>apps/web/src/shared/api/client.ts</files>
  <read_first>apps/web/src/shared/api/client.ts</read_first>
  <action>
    Ensure fetchJson throws Error including HTTP status when res.status === 404 so detail page can branch to not-found UI.
  </action>
  <acceptance_criteria>
    - fetchJson throws on !res.ok with message containing 404 for not found responses
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <done>404 distinguishable from network errors</done>
</task>

</tasks>

<verification>
- [ ] Manual: open product from grid — detail shows image, price, description
- [ ] Manual: add-to-cart button is disabled and labeled coming soon
- [ ] Manual: /products/invalid-uuid shows not found + link back
</verification>
