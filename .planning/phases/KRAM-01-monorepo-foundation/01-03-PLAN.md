---
phase: 01-monorepo-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - docker-compose.yml
  - package.json
  - README.md
  - tests/foundation/docker-compose-config.test.ts
  - tests/foundation/readme-dev-flow.test.ts
  - tests/foundation/postgres-scripts.test.ts
  - scripts/verify-postgres.sh
  - scripts/verify-postgres.ps1
  - scripts/verify-phase1.sh
  - scripts/verify-phase1.ps1
autonomous: true
requirements:
  - FOUND-03
user_setup: []

must_haves:
  truths:
    - "D-09: Postgres not started by turbo dev; D-13: manual pnpm db:up; D-14: postgres/postgres shop_dev; D-15: root docker-compose 5432:5432"
    - "Developer runs `pnpm db:up` from repo root and PostgreSQL `shop_dev` listens on localhost:5432"
    - "`pnpm db:down` stops the Compose stack without affecting `pnpm dev` processes"
    - "Root README documents install → db:up → dev, ports 5173/3000/5432, and that Postgres is not started by `turbo dev`"
    - "`apps/api/.env.example` DATABASE_URL matches Docker credentials (postgres/postgres/shop_dev)"
  artifacts:
    - path: docker-compose.yml
      provides: Local PostgreSQL 16 for development
      contains: "shop_dev"
    - path: README.md
      provides: Documented monorepo dev workflow
      contains: "pnpm db:up"
    - path: tests/foundation/docker-compose-config.test.ts
      provides: Automated FOUND-03 compose contract checks
      contains: "5432:5432"
  key_links:
    - from: package.json
      to: docker-compose.yml
      via: scripts db:up and db:down
      pattern: "docker compose"
    - from: apps/api/.env.example
      to: docker-compose.yml
      via: DATABASE_URL host port and database name
      pattern: "shop_dev"
    - from: turbo.json
      to: docker-compose.yml
      via: dev task does NOT depend on db or compose
      pattern: "dependsOn"
---

## Phase Goal

**As a** developer, **I want to** start PostgreSQL with one command and follow documented steps for the full local stack, **so that** Phase 2 Prisma can connect to a known `shop_dev` database without renegotiating dev infrastructure.

<objective>
Add root Docker Compose for PostgreSQL, confirm `pnpm db:up` / `db:down` scripts, document the walking-skeleton dev flow in README, and add Vitest + shell smoke checks for compose config and `pg_isready`.

Purpose: FOUND-03 and walking-skeleton completion (D-09, D-13–D-15, SKELETON.md).
Output: `pnpm install` → `pnpm db:up` → `pnpm dev` documented with healthy Postgres on 5432.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/KRAM-01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md
@.planning/phases/KRAM-01-monorepo-foundation/01-PATTERNS.md
@.planning/phases/KRAM-01-monorepo-foundation/01-SKELETON.md
@.planning/phases/KRAM-01-monorepo-foundation/01-VALIDATION.md
@package.json
@apps/api/.env.example
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root Docker Compose and db scripts</name>
  <read_first>
    - .planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md — Pattern 6
    - .planning/phases/KRAM-01-monorepo-foundation/01-CONTEXT.md — D-09, D-13, D-14, D-15
    - package.json
    - turbo.json
  </read_first>
  <files>
    docker-compose.yml
    package.json
    tests/foundation/docker-compose-config.test.ts
  </files>
  <action>
    Per D-13, D-14, D-15: create `docker-compose.yml` at repo root with single service `postgres`, image `postgres:16`, environment `POSTGRES_USER=postgres`, `POSTGRES_PASSWORD=postgres`, `POSTGRES_DB=shop_dev`, ports `5432:5432`, volume `postgres_data:/var/lib/postgresql/data`, and top-level `volumes: postgres_data:`. Do not add app services or Nest containers. Per D-09: do NOT wire Compose into `turbo.json` or add `dependsOn` for database — `turbo dev` must remain web + API only. Ensure root `package.json` scripts are exactly `db:up`: `docker compose up -d` and `db:down`: `docker compose down` (idempotent if already set in plan 01-01). Add a short comment at top of `docker-compose.yml`: dev-only tutorial credentials per D-14 — never use in production. Add `tests/foundation/docker-compose-config.test.ts` that reads `docker-compose.yml` as text (no new npm deps — string assertions only) and asserts contains `postgres:16`, `shop_dev`, `5432:5432`, `POSTGRES_USER: postgres`, and `postgres_data` volume. Run `pnpm db:up` then `docker compose exec -T postgres pg_isready -U postgres` for manual gate; run `pnpm db:down` after.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/docker-compose-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists at repo root with service name `postgres` and image `postgres:16`
    - Compose file sets `POSTGRES_DB: shop_dev` and port mapping `5432:5432`
    - Root `package.json` contains scripts `db:up` and `db:down` invoking `docker compose`
    - `turbo.json` `dev` task has no docker/db dependency
    - After `pnpm db:up`, `docker compose exec -T postgres pg_isready -U postgres` exits 0
  </acceptance_criteria>
  <done>Postgres container starts via pnpm db:up with locked credentials and port</done>
</task>

<task type="auto">
  <name>Task 2: README local development guide</name>
  <read_first>
    - .planning/phases/KRAM-01-monorepo-foundation/01-PATTERNS.md — README.md section
    - .planning/phases/KRAM-01-monorepo-foundation/01-SKELETON.md — capability proven
    - .planning/PROJECT.md — Angular learner note
    - README.md
    - apps/api/.env.example
  </read_first>
  <files>
    README.md
    tests/foundation/readme-dev-flow.test.ts
  </files>
  <action>
    Per FOUND-02/03 and D-09: add or replace a **Local development** section in root `README.md`. Preserve any existing `&lt;!-- GSD:... --&gt;` comment blocks verbatim if present; replace Vite template marketing/ESLint expansion prose with Kramnik Shop monorepo instructions. Document ordered flow: (1) `corepack enable` and `corepack prepare pnpm@9.15.9 --activate`, (2) `pnpm install`, (3) `pnpm db:up` — note Postgres is **not** started by `pnpm dev` / `turbo dev` (D-09), (4) copy `apps/api/.env.example` to `apps/api/.env` when working with API/DB, (5) `pnpm dev` for web + API. List ports: web **5173**, API **3000**, Postgres **5432**. Include **Security (dev only)** subsection per D-14: default user/password `postgres` and database `shop_dev` are tutorial credentials for local Docker only — document risk of port 5432 exposure on shared machines and advise stopping with `pnpm db:down`. Mention optional `scripts/verify-phase1.sh` / `.ps1` phase gate (added in Task 3). Include **Angular background** note: `pnpm dev` / `turbo dev` ≈ running `ng serve` for web and API in parallel via Turborepo. Point learners to `.planning/PROJECT.md` for stack mapping. Mention port conflict troubleshooting if local Postgres already binds 5432 (stop host service; do not change D-15 mapping without explicit decision change). Cross-check `DATABASE_URL` in README matches `postgresql://postgres:postgres@localhost:5432/shop_dev` in `apps/api/.env.example`. Add `tests/foundation/readme-dev-flow.test.ts` (Vitest, reads `README.md` as text) asserting content includes `pnpm db:up`, `pnpm dev`, ports `5173`, `3000`, and `5432`, and a sentence that `turbo dev` does not start Postgres (case-insensitive substring match acceptable).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/readme-dev-flow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - README contains `pnpm db:up`, `pnpm dev`, and explicit note that turbo dev does not start Postgres
    - README lists ports 5173, 3000, and 5432
    - README includes dev-only credential warning for postgres/postgres/shop_dev
    - README includes Angular/Turborepo comparison sentence
    - `tests/foundation/readme-dev-flow.test.ts` passes on committed README
  </acceptance_criteria>
  <done>FOUND-03 dev flow documented for walking-skeleton onboarding</done>
</task>

<task type="auto">
  <name>Task 3: pg_isready smoke scripts for phase gate</name>
  <read_first>
    - .planning/phases/KRAM-01-monorepo-foundation/01-VALIDATION.md — 01-03-01 row
    - .planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md — Validation Architecture FOUND-03
    - scripts/verify-api-health.sh
    - scripts/verify-api-health.ps1
  </read_first>
  <files>
    scripts/verify-postgres.sh
    scripts/verify-postgres.ps1
    scripts/verify-phase1.sh
    scripts/verify-phase1.ps1
    tests/foundation/postgres-scripts.test.ts
  </files>
  <action>
    Add `scripts/verify-postgres.sh`: precondition comment that operator ran `pnpm db:up`; runs `docker compose exec -T postgres pg_isready -U postgres` and exits non-zero on failure. Add `scripts/verify-postgres.ps1` with equivalent `docker compose exec` via PowerShell. Mirror structure of `scripts/verify-api-health.*` from plan 01-02. Add `scripts/verify-phase1.sh` and `scripts/verify-phase1.ps1` that chain API health scripts then postgres scripts (call sibling scripts in order; document preconditions: API running for health step, `pnpm db:up` for postgres step). Reference `verify-phase1` in README Local development section as optional phase gate. Add `tests/foundation/postgres-scripts.test.ts` (Vitest, reads script files as text) asserting `scripts/verify-postgres.sh` and `scripts/verify-postgres.ps1` exist and both reference `pg_isready` and service `postgres`. Do not use watch mode. Script headers state Docker must be running and compose stack up — suitable for phase gate / manual FOUND-03 verification when Vitest alone is insufficient.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/postgres-scripts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-postgres.sh` invokes `pg_isready` against service `postgres`
    - `scripts/verify-postgres.ps1` exists with equivalent check
    - `scripts/verify-phase1.sh` and `scripts/verify-phase1.ps1` chain API + postgres checks
    - `tests/foundation/postgres-scripts.test.ts` passes
    - With stack up, `bash scripts/verify-postgres.sh` exits 0 (manual gate)
  </acceptance_criteria>
  <done>FOUND-03 pg_isready smoke scripts ready for phase gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker daemon | `pnpm db:up` starts container with published 5432 |
| Developer → README | Tutorial credentials documented as dev-only |
| CI → Docker | Vitest compose tests are file-static; pg_isready scripts optional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information disclosure | Postgres tutorial credentials (D-14) | mitigate | README Security subsection: dev-only; never reuse in production; `pnpm db:down` on shared machines |
| T-01-07 | Tampering | `postgres:16` image pull | mitigate | Pin major tag `postgres:16`; document Docker Hub official image; verify digest in team runbooks if policy requires |
| T-01-08 | Denial of service | Host port 5432 bind | accept | Document local port conflict; D-15 locks mapping — stop conflicting host Postgres |
| T-01-09 | Information disclosure | Published 5432 on LAN | mitigate | Document firewall awareness; default bind is all interfaces via Docker — note risk in README for shared networks |
| T-01-02 | Information disclosure | `apps/api/.env` | mitigate | Reconfirm `.env` gitignored (plan 01-01); README instructs copy from `.env.example` only |
</threat_model>

<verification>
- `pnpm exec vitest run tests/foundation/docker-compose-config.test.ts` passes
- `pnpm db:up` then `docker compose exec -T postgres pg_isready -U postgres` succeeds
- `pnpm dev` does not start or stop Postgres (manual: only web + API processes)
- README flow matches SKELETON.md: install → db:up → dev
</verification>

<success_criteria>
- FOUND-03: PostgreSQL runs locally via Docker Compose with connection string documented
- Walking skeleton: developer can reach web, API, and healthy Postgres using documented commands
- D-13, D-14, D-15 implemented; D-09 separation from turbo dev documented
</success_criteria>

<output>
Create `.planning/phases/KRAM-01-monorepo-foundation/01-03-SUMMARY.md` when done
</output>
