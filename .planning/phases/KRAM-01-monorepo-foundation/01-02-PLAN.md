---
phase: 01-monorepo-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/tsconfig.build.json
  - apps/api/nest-cli.json
  - apps/api/eslint.config.js
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/app.controller.ts
  - apps/api/.env.example
  - apps/web/package.json
  - turbo.json
  - scripts/verify-api-health.ps1
  - scripts/verify-api-health.sh
  - tests/foundation/api-health-scripts.test.ts
autonomous: true
requirements:
  - FOUND-02
user_setup: []

must_haves:
  truths:
    - "D-08: pnpm dev runs turbo dev for web + API; D-10: ports 5173 and 3000"
    - "D-11: DATABASE_URL in apps/api/.env.example; D-12: CORS origin http://localhost:5173 only"
    - "D-23: GET / and GET /health return JSON"
    - "Developer runs `pnpm dev` from repo root and both web (5173) and API (3000) start after @kramnik/types#build"
    - "GET http://localhost:3000/ returns JSON welcome message"
    - "GET http://localhost:3000/health returns JSON with status ok"
    - "Nest CORS allows only http://localhost:5173"
  artifacts:
    - path: apps/api/src/main.ts
      provides: Nest bootstrap with CORS
      contains: "enableCors"
    - path: apps/api/src/app.controller.ts
      provides: Root and health routes
      contains: "health"
    - path: apps/api/.env.example
      provides: Documented DATABASE_URL for Phase 2
      contains: "postgresql://postgres:postgres@localhost:5432/shop_dev"
  key_links:
    - from: turbo.json
      to: apps/api
      via: dev task includes api in turbo dev graph
      pattern: "persistent"
    - from: apps/api/src/main.ts
      to: apps/web
      via: CORS origin localhost:5173
      pattern: "localhost:5173"

---

## Phase Goal

**As a** developer, **I want to** run web and API together with one command from the repo root, **so that** I can verify the full client/server dev loop before adding Prisma.

<objective>
Scaffold NestJS 11 in `apps/api` with welcome and health endpoints, CORS for Vite, and wire `apps/api#dev` into the existing Turbo `dev` pipeline alongside `@kramnik/web#dev`.

Purpose: FOUND-02 documented dev orchestration (D-08, D-10–D-12, D-23).
Output: `pnpm dev` runs types build then parallel web + API.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/KRAM-01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md
@.planning/phases/KRAM-01-monorepo-foundation/01-PATTERNS.md
@.planning/phases/KRAM-01-monorepo-foundation/01-SKELETON.md
@turbo.json
@apps/web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold NestJS app in apps/api</name>
  <read_first>
    - .planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md — Pattern 4
    - .planning/phases/KRAM-01-monorepo-foundation/01-PATTERNS.md — apps/api rows
    - turbo.json
    - tsconfig.base.json
    - eslint.config.js
  </read_first>
  <files>
    apps/api/package.json
    apps/api/tsconfig.json
    apps/api/tsconfig.build.json
    apps/api/nest-cli.json
    apps/api/eslint.config.js
    apps/api/src/main.ts
    apps/api/src/app.module.ts
    apps/api/src/app.controller.ts
    apps/api/.env.example
  </files>
  <action>
    Create `apps/api` with `name: "@kramnik/api"`, `private: true`. Use `@nestjs/cli` as devDependency and scaffold via `pnpm exec nest new api --directory apps/api --skip-git --package-manager pnpm` from repo root OR hand-create equivalent structure if CLI is interactive on Windows — output MUST land in `apps/api` not repo root. Dependencies: `@nestjs/common`, `@nestjs/core`, `@nestjs/platform-express`, `reflect-metadata`, `rxjs`. DevDependencies: `@nestjs/cli`, `@types/node`, `typescript` ~6.0.2, `eslint`. Scripts: `dev: nest start --watch`, `build: nest build`, `start: node dist/main`, `lint: eslint .`. `tsconfig.json` extends `../../tsconfig.base.json` with `module: commonjs`, `emitDecoratorMetadata: true`, `experimentalDecorators: true`, `outDir: dist`, `noEmit: false`. `apps/api/eslint.config.js` imports root shared config + `globals.node`. Per D-11/D-23: create `apps/api/.env.example` with `PORT=3000` and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shop_dev`. Implement `main.ts` with `enableCors({ origin: 'http://localhost:5173' })` and `listen(process.env.PORT ?? 3000)`. Implement `AppController` with `GET /` returning `{ message: 'Kramnik Shop API' }` and `GET /health` returning `{ status: 'ok' }`. Remove any default Nest demo HTML if CLI added it. Do NOT add Prisma or connect to DB.
  </action>
  <verify>
    <automated>pnpm --filter @kramnik/api build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/dist/main.js` exists after build
    - `apps/api/src/app.controller.ts` contains `@Get('health')`
    - `apps/api/src/main.ts` contains `enableCors` and origin `http://localhost:5173`
    - `apps/api/.env.example` contains exact DATABASE_URL from D-14
    - No `apps/api/.env` committed
  </acceptance_criteria>
  <done>Nest API builds and controller routes defined</done>
</task>

<task type="auto">
  <name>Task 2: Wire turbo dev for web + api</name>
  <read_first>
    - turbo.json
    - apps/web/package.json
    - apps/api/package.json
    - .planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md — Pattern 2
  </read_first>
  <files>
    turbo.json
    apps/web/package.json
    apps/api/package.json
  </files>
  <action>
  Ensure both `apps/web` and `apps/api` declare `"dev"` script. Confirm `turbo.json` `dev` task has `dependsOn: ["@kramnik/types#build"]` only (no `^dev`). Running `pnpm dev` at root must invoke `turbo dev` which starts persistent dev for both packages in parallel after types build. Optionally add `workspace:*` dependency from `@kramnik/web` or `@kramnik/api` to `@kramnik/types` only if needed for Turbo graph — not required for imports in Phase 1. Verify `pnpm --filter @kramnik/api dev` listens on port 3000 when run alone.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/turbo-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Root `package.json` script `dev` equals `turbo dev`
    - `turbo.json` dev.dependsOn includes `@kramnik/types#build` and not `^dev`
    - Both workspace packages have dev scripts
  </acceptance_criteria>
  <done>Single root command orchestrates web + API dev</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-verify API endpoints</name>
  <read_first>
    - apps/api/src/app.controller.ts
    - .planning/phases/KRAM-01-monorepo-foundation/01-VALIDATION.md
  </read_first>
  <files>
    scripts/verify-api-health.ps1
    scripts/verify-api-health.sh
    tests/foundation/api-health-scripts.test.ts
  </files>
  <action>
    Add cross-platform smoke scripts: start is manual (`pnpm dev` in separate terminal). Scripts MUST target `http://localhost:3000/health` first (primary FOUND-02 gate) and also hit `http://localhost:3000/` for welcome JSON. Shell script uses `curl -sf` against `/health` expecting body containing `"ok"`; PowerShell uses `Invoke-WebRequest` against `/health` with equivalent assertions. Document in each script header that API must already be running on port 3000. Do not add watch-mode or live-server Vitest tests. Add `tests/foundation/api-health-scripts.test.ts` (Vitest, Node `fs.readFileSync`) that asserts both script files exist and their text contains `localhost:3000/health` (or `3000` + `/health` for PowerShell) and references port `3000` — no duplicate `pnpm --filter @kramnik/api build` gate here (build is covered in Task 1).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/api-health-scripts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-api-health.sh` exists and contains `localhost:3000/health`
    - `scripts/verify-api-health.ps1` exists and contains `3000` and `health`
    - `tests/foundation/api-health-scripts.test.ts` passes and asserts both scripts exist and target `/health`
    - Scripts exit non-zero when API is down (documented manual precondition)
  </acceptance_criteria>
  <done>FOUND-02 API verification scripts ready for phase gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | CORS restricts origin to local Vite dev server |
| Developer → API env | Secrets only in gitignored `.env` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | CORS | mitigate | `origin: 'http://localhost:5173'` only — no wildcard |
| T-01-02 | Information disclosure | DATABASE_URL in .env | mitigate | `.env.example` only; `.env` gitignored from plan 01-01 |
| T-01-05 | Information disclosure | Health endpoint | accept | Public `{ status: 'ok' }` is intentional for dev |
</threat_model>

<verification>
- `pnpm dev` starts web and API (manual: curl health + open 5173)
- `pnpm --filter @kramnik/api build` passes
- CORS header present on API responses from browser origin test (optional manual)
</verification>

<success_criteria>
- FOUND-02: documented `pnpm dev` runs web + API from repo root
- API exposes `/` and `/health` per D-23
</success_criteria>

<output>
Create `.planning/phases/KRAM-01-monorepo-foundation/01-02-SUMMARY.md` when done
</output>
