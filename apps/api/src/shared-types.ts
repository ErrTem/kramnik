/**
 * Phase 3: re-export shared DTOs — catalog/order responses must use these shapes.
 * Do not hand-write Product/User interfaces in apps (see README).
 */
export type { ProductPublic, UserPublic, Category, Role } from '@kramnik/types';
