---
phase: 05-catalog-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/web/src/features/catalog/ProductListPage.tsx
  - apps/web/src/features/catalog/ProductCard.tsx
  - apps/web/src/features/catalog/ProductFilters.tsx
  - apps/web/src/shared/lib/categoryLabel.ts
  - tests/web/format-price.test.ts
  - tests/web/products-query.test.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-04
  - LEARN-02
  - LEARN-03
user_setup: []

must_haves:
  truths:
    - "D-01: responsive card grid 1/2/3-4 columns"
    - "D-02: card shows image, name, price, category badge"
    - "D-03: 4:3 image box object-cover; placeholder on error"
    - "D-04: whole card Link to /products/:id"
    - "D-09: horizontal filter bar above grid"
    - "D-10: debounced search ~300ms"
    - "D-11: useQuery key ['products', filter object]"
    - "D-12: filters synced to URL searchParams"
    - "D-13: no pagination — full list rendered"
    - "D-16: skeleton cards while loading"
    - "D-17: inline error with refetch"
    - "User can browse and filter products against live API"
  artifacts:
    - path: apps/web/src/features/catalog/ProductCard.tsx
      provides: Grid card UI
      contains: "Link"
    - path: apps/web/src/features/catalog/ProductFilters.tsx
      provides: Filter bar
      contains: "category"
  key_links:
    - from: ProductListPage.tsx
      to: shared/api/products.ts
      via: useQuery queryFn fetchProducts
      pattern: "useQuery"

threat_model:
  threats:
    - id: T-05-02
      stride: Information Disclosure
      description: XSS via unsanitized product description on list (if ever shown)
      mitigation: List cards omit description; only text children for name/price
      severity: low
---

## Phase Goal

**As a** shopper, **I want** to browse products in a filterable grid, **so that** I can find items by category, search, or price (CAT-01, CAT-02, CAT-04).

<objective>
Implement ProductListPage with useQuery, URL-synced filters, ProductCard grid per UI-SPEC, loading skeletons, and error retry.

Depends on: 05-01 shell and API helpers.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/KRAM-05-catalog-ui/05-CONTEXT.md
@.planning/phases/KRAM-05-catalog-ui/05-UI-SPEC.md
@.planning/phases/KRAM-05-catalog-ui/05-RESEARCH.md
@apps/web/src/shared/api/products.ts
@apps/web/src/features/catalog/ProductListPage.tsx
@packages/types/src/index.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: categoryLabel helper and Wave 0 tests</name>
  <files>apps/web/src/shared/lib/categoryLabel.ts, tests/web/format-price.test.ts, tests/web/products-query.test.ts</files>
  <read_first>packages/types/src/product.ts, apps/web/src/shared/lib/formatPrice.ts, vitest.config.ts</read_first>
  <action>
    categoryLabel.ts maps Category enum to human labels (Home, Garbage, Scraps, Garden Gnomes).
    tests/web/format-price.test.ts imports formatPrice and asserts USD output for sample string.
    tests/web/products-query.test.ts tests query string builder used by fetchProducts (category=HOME when set).
    Export builder from products.ts if needed for testability.
  </action>
  <acceptance_criteria>
    - categoryLabel('GARDEN_GNOMES') returns readable label containing Gnome or Garden
    - pnpm test runs and new tests pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Helpers and tests green</done>
</task>

<task type="execute">
  <name>Task 2: ProductFilters with URL sync and debounce</name>
  <files>apps/web/src/features/catalog/ProductFilters.tsx</files>
  <read_first>05-CONTEXT.md D-09 through D-12, ListProductsQueryDto fields</read_first>
  <action>
    ProductFilters: controlled inputs for category (all + four enum values), search q, minPrice, maxPrice.
    useSearchParams: initialize state from URL on mount; update URL with replace:true when filters change; omit empty params.
    Debounce q updates 300ms (useEffect + timeout or useDeferredValue pattern).
    Expose filter state object to parent via props callback or render props.
  </action>
  <acceptance_criteria>
    - Changing category updates browser URL search param category=
    - Typing in search does not hit API on every keystroke (debounced)
    - All four Category values selectable
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <done>Filters wired to URL</done>
</task>

<task type="execute">
  <name>Task 3: ProductCard component</name>
  <files>apps/web/src/features/catalog/ProductCard.tsx</files>
  <read_first>05-UI-SPEC.md product card section, 05-CONTEXT D-01 to D-04</read_first>
  <action>
    ProductCard receives ProductPublic prop.
    Outer element: Link to `/products/${product.id}` wrapping entire card (focus-visible ring).
    Image area: aspect-[4/3] w-full object-cover; onError set state to show gray placeholder div.
    Show name, formatPrice(price), category badge using categoryLabel.
    Tailwind grid item styling: rounded border shadow-sm hover:shadow-md transition.
  </action>
  <acceptance_criteria>
    - ProductCard.tsx contains Link and aspect ratio class for 4:3
    - Badge displays category label text
    - No dangerouslySetInnerHTML
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <done>Card matches CONTEXT layout decisions</done>
</task>

<task type="execute">
  <name>Task 4: ProductListPage with useQuery</name>
  <files>apps/web/src/features/catalog/ProductListPage.tsx</files>
  <read_first>apps/web/src/shared/api/products.ts, 05-RESEARCH.md TanStack patterns</read_first>
  <action>
    ProductListPage composes ProductFilters + grid.
    useQuery with queryKey ['products', filters] and queryFn () => fetchProducts(filters).
    isPending: render 6 skeleton card placeholders matching grid columns.
    isError: show message and button calling refetch().
    isSuccess empty array: message No products match your filters.
    isSuccess: CSS grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 mapping ProductCard.
  </action>
  <acceptance_criteria>
    - ProductListPage.tsx contains useQuery and queryKey including products
    - With API running and seed data, /products shows at least one product card
    - Filter by category HOME reduces visible cards when only HOME products exist
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <done>CAT-01 CAT-02 CAT-04 list flow complete</done>
</task>

</tasks>

<verification>
- [ ] Manual: /products shows grid of seeded products
- [ ] Manual: ?category=GARDEN_GNOMES filters URL and results
- [ ] Manual: Stop API — error UI shows Retry
</verification>
