---
phase: 02-prisma-data-model
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/prisma/seed.ts
  - apps/api/package.json
  - README.md
  - scripts/verify-phase2.sh
  - scripts/verify-phase2.ps1
  - tests/data/seed-content.test.ts
  - tests/data/readme-db-flow.test.ts
  - tests/foundation/verify-phase2-scripts.test.ts
autonomous: true
requirements:
  - DATA-03
user_setup: []

must_haves:
  truths:
    - "D-06/D-07: seed.ts at apps/api/prisma/seed.ts; Phase 3 types path ../../apps/api/prisma/schema.prisma documented in README"
    - "D-10: seed imageUrl uses external placeholder URLs; D-11: all products visible (no publish flags)"
    - "D-15: README notes Decimal maps to string in public JSON in Phase 3+ (PITFALLS §6)"
    - "D-19: seed admin@kramnik.local and customer@kramnik.local with bcrypt hashes"
    - "D-23: seed creates no Order rows"
    - "D-25–D-26: ~12 themed products across HOME, GARBAGE, SCRAPS, GARDEN_GNOMES"
    - "D-27: idempotent upsert by email (users) and slug (products)"
    - "D-28: README dev-only warning for Admin123!/Customer123! tutorial passwords"
    - "D-29: README flow db:up → .env → db:migrate → db:seed → dev; optional db:studio/db:reset"
    - "D-30: pnpm db:reset runs prisma migrate reset (reapply + seed on Prisma 6)"
    - "D-31: bcryptjs hashes passwords in seed (cost ≥10)"
    - "D-32: verify-phase2 scripts confirm ≥12 products and admin user"
    - "pnpm db:seed populates shop_dev; verify-phase2 passes with Docker up"
  artifacts:
    - path: apps/api/prisma/seed.ts
      provides: DATA-03 idempotent seed
      contains: "upsert"
    - path: scripts/verify-phase2.sh
      provides: Phase 2 DB smoke gate
      contains: "admin@kramnik.local"
    - path: README.md
      provides: Documented migrate/seed flow and dev logins
      contains: "pnpm db:seed"
    - path: tests/data/seed-content.test.ts
      provides: Static seed security contract
      contains: "passwordHash"
  key_links:
    - from: apps/api/prisma/seed.ts
      to: "@prisma/client"
      via: PrismaClient upsert
      pattern: "prisma\\.(user|product)\\.upsert"
    - from: package.json
      to: apps/api/prisma/seed.ts
      via: db:seed → prisma db seed
      pattern: "db:seed"
    - from: scripts/verify-phase2.sh
      to: docker-compose postgres
      via: psql COUNT Product
      pattern: "shop_dev"
---

## Phase Goal

**As a** developer, **I want to** seed sample products and dev users and verify the database with documented commands, **so that** later phases can build catalog and auth against real data.

<objective>
Implement idempotent `seed.ts` with bcryptjs, extend README with full DB workflow and dev credentials, add verify-phase2 scripts and Vitest static checks, and run seed against migrated `shop_dev`.

Purpose: DATA-03 and developer workflow (D-19, D-25–D-32, D-28, D-29).
Output: Populated database, phase gate scripts, README db section.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/KRAM-02-prisma-data-model/02-CONTEXT.md
@.planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md
@.planning/phases/KRAM-02-prisma-data-model/02-PATTERNS.md
@.planning/phases/KRAM-02-prisma-data-model/02-VALIDATION.md
@.planning/phases/KRAM-01-monorepo-foundation/01-03-PLAN.md
@apps/api/prisma/schema.prisma
@README.md
@scripts/verify-postgres.sh
@scripts/verify-phase1.sh
@tests/foundation/postgres-scripts.test.ts
@tests/foundation/readme-dev-flow.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — seed, README, and verify-phase2 static tests</name>
  <read_first>
    - .planning/phases/KRAM-02-prisma-data-model/02-VALIDATION.md — Wave 0 Requirements
    - .planning/phases/KRAM-02-prisma-data-model/02-PATTERNS.md — verify-phase2, readme, seed tests
    - tests/foundation/readme-dev-flow.test.ts
    - tests/foundation/postgres-scripts.test.ts
  </read_first>
  <files>
    tests/data/seed-content.test.ts
    tests/data/readme-db-flow.test.ts
    tests/foundation/verify-phase2-scripts.test.ts
    scripts/verify-phase2.sh
    scripts/verify-phase2.ps1
  </files>
  <behavior>
    - Test seed-content: seed.ts uses bcryptjs/bcrypt, upsert, admin@kramnik.local, passwordHash, no prisma.order.create, no plaintext Admin123! assigned to a DB column literal
    - Test readme-db-flow: README contains pnpm db:migrate, pnpm db:seed, admin@kramnik.local, dev-only password warning
    - Test verify-phase2-scripts: sh/ps1 exist, chain verify-postgres, reference Product count and admin email
  </behavior>
  <action>
    Per D-32: add `scripts/verify-phase2.sh` — precondition comment: `pnpm db:up`, migrate and seed already run; `set -euo pipefail`; chain `scripts/verify-postgres.sh` (no API health — DB-only phase gate per PATTERNS). Then `docker compose exec -T postgres psql -U postgres -d shop_dev -tAc` query `SELECT COUNT(*)::int FROM "Product"` — fail if count &lt; 12. Query `SELECT 1 FROM "User" WHERE email='admin@kramnik.local' LIMIT 1` — must return row. Mirror in `scripts/verify-phase2.ps1` with `$ErrorActionPreference = 'Stop'` and exit checks. Add `tests/foundation/verify-phase2-scripts.test.ts` like postgres-scripts.test.ts: assert scripts exist, contain verify-postgres or pg_isready, Product, admin@kramnik.local, shop_dev. Add `tests/data/seed-content.test.ts` (reads seed.ts — file may not exist yet; create minimal stub seed.ts with comment only if needed for test file path, or write tests first expecting future content). Add `tests/data/readme-db-flow.test.ts` extending readme-dev-flow pattern for db:migrate, db:seed, db:studio or db:reset, dev credentials subsection. Do NOT implement full seed or README prose in this task beyond stubs required for tests to be structurally valid — full content in Task 2–3. If tests require committed strings, add placeholder README lines and minimal seed.ts export in Task 2. Prefer: write tests asserting target strings; implement seed/README in following tasks until green.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/verify-phase2-scripts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - verify-phase2.sh and .ps1 exist with postgres chain and SQL checks
    - verify-phase2-scripts.test.ts passes (seed-content and readme-db-flow tests run in Tasks 2–3)
    - Scripts follow Phase 1 conventions (bash shebang, set -euo pipefail)
  </acceptance_criteria>
  <done>Wave 0 test and script scaffolding in place</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent seed with bcryptjs and db:seed</name>
  <read_first>
    - .planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md — Pattern 3 (upsert seed)
    - apps/api/prisma/schema.prisma
  </read_first>
  <files>
    apps/api/prisma/seed.ts
    apps/api/package.json
  </files>
  <behavior>
    - Test seed-content: bcryptjs hash calls, upsert on email/slug, ≥12 product slugs in file or data array, no order creates
    - After seed: verify-phase2 product count ≥12 when DB up
  </behavior>
  <action>
    Per D-06, D-19, D-23, D-25–D-28, D-31: create `apps/api/prisma/seed.ts` run via `tsx prisma/seed.ts`. Import PrismaClient, Prisma, Role, Category from `@prisma/client`; import bcrypt from `bcryptjs`. Use `const prisma = new PrismaClient()` with main/catch/finally disconnect per RESEARCH. Hash **Admin123!** and **Customer123!** with bcrypt cost **10**; store only in **passwordHash** (D-17). Upsert users: `admin@kramnik.local` role ADMIN, `customer@kramnik.local` role CUSTOMER (D-19, D-27). Upsert **≥12 products** (D-25) with playful themed names/descriptions (D-26) spread across all four categories (D-08); unique **slug** per product; **price** as `new Prisma.Decimal('…')` (D-12); **imageUrl** external placeholders e.g. placehold.co (D-10). **No** `prisma.order.create` (D-23). Idempotent: `upsert` where email/slug (D-27). From repo root with `pnpm db:up` and migrated DB: run `pnpm db:seed`. Re-run seed twice to confirm idempotency (no duplicate slug/email errors).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/data/seed-content.test.ts && pnpm db:seed && pnpm db:seed && bash scripts/verify-phase2.sh</automated>
  </verify>
  <acceptance_criteria>
    - seed.ts uses bcryptjs and upsert only; no plaintext passwords persisted
    - `pnpm db:seed` exits 0 twice in a row (idempotent)
    - `bash scripts/verify-phase2.sh` exits 0 with ≥12 Product rows and admin@kramnik.local present (precondition: `pnpm db:up` and 02-01 migration applied)
    - tests/data/seed-content.test.ts passes
  </acceptance_criteria>
  <done>DATA-03 seed data in database</done>
</task>

<task type="auto">
  <name>Task 3: README db workflow, dev credentials, and phase gate</name>
  <read_first>
    - .planning/phases/KRAM-02-prisma-data-model/02-PATTERNS.md — README section
    - README.md — preserve GSD comments if any
    - .planning/phases/KRAM-02-prisma-data-model/02-CONTEXT.md — D-29, D-28, D-15, D-07
  </read_first>
  <files>
    README.md
  </files>
  <action>
    Per D-29, D-28, D-15, D-07, D-30: update **Local development** in root README. Preserve any `&lt;!-- GSD:... --&gt;` blocks verbatim. Insert ordered steps after copy `.env`: `pnpm db:migrate`, `pnpm db:seed`, then `pnpm dev`. Document optional `pnpm db:studio`, `pnpm db:reset` (D-30 — reapplies migrations and seed on Prisma 6). Add **Scripts** table rows for db:migrate, db:seed, db:studio, db:reset, db:generate. Add **Dev app logins (dev only)** subsection mirroring **Security (dev only)** tone (D-28): `admin@kramnik.local` / `Admin123!`, `customer@kramnik.local` / `Customer123!` — tutorial passwords, never production. Note **Phase 3 types**: `packages/types` will generate from `apps/api/prisma/schema.prisma` (D-07). Note **Decimals**: API/UI will expose prices as strings in JSON (D-15). Reference optional `scripts/verify-phase2.sh` / `.ps1` as Phase 2 gate (after db:up, migrate, seed). Angular learner one-liner optional: seed ≈ DbInitializer. Run `pnpm exec vitest run tests/data/readme-db-flow.test.ts`. With Docker up and seed applied, run `bash scripts/verify-phase2.sh` (or `.ps1` on Windows).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/data/readme-db-flow.test.ts && bash scripts/verify-phase2.sh</automated>
  </verify>
  <acceptance_criteria>
    - README documents full db:up → .env → db:migrate → db:seed → dev flow
    - Dev credentials labeled dev-only with security warning
    - `tests/data/readme-db-flow.test.ts` passes
    - `bash scripts/verify-phase2.sh` exits 0 (regression gate after README edits)
  </acceptance_criteria>
  <done>Phase 2 developer workflow documented and verifiable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed.ts → PostgreSQL | Writes passwordHash and catalog rows to local shop_dev |
| README → developer | Documents tutorial credentials (information disclosure if misused) |
| verify scripts → Docker | Read-only SQL counts against dev database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information disclosure | Dev passwords in README (D-28) | mitigate | Dedicated **dev only** subsection; never use in production; mirror Postgres tutorial warning |
| T-02-01 | Information disclosure | Plaintext passwords in DB | mitigate | D-17/D-31: bcryptjs cost 10 → passwordHash only; seed-content test forbids plaintext column assignment |
| T-02-06 | Tampering | Seed re-run duplicates | mitigate | D-27 upsert on unique email/slug |
| T-02-03 | Tampering | Product delete breaks orders | mitigate | Schema Restrict (02-01); seed does not delete products |
| T-02-07 | Spoofing | Weak dev passwords | accept | Tutorial credentials for local learning only; documented as non-production (D-28) |
| T-02-08 | Information disclosure | verify-phase2 SQL | accept | Local Docker only; scripts document precondition |
</threat_model>

<verification>
- `pnpm exec vitest run tests/data tests/foundation/verify-phase2-scripts.test.ts` passes
- `pnpm db:up` → `pnpm db:seed` → `bash scripts/verify-phase2.sh` exits 0
- `pnpm db:reset` reapplies migrations and seed (manual optional per VALIDATION.md)
- No PrismaModule, REST, or packages/types added
</verification>

<success_criteria>
- DATA-03: ≥12 products and admin user in shop_dev after seed
- Developer can follow README to migrate and seed from fresh clone
- D-25–D-32 and D-28, D-29 workflow complete
</success_criteria>

<output>
Create `.planning/phases/KRAM-02-prisma-data-model/02-02-SUMMARY.md` when done
</output>
