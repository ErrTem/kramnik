---
phase: 02-prisma-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - apps/api/package.json
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations
  - tests/data/prisma-schema.test.ts
autonomous: false
requirements:
  - DATA-01
  - DATA-02
user_setup: []

must_haves:
  truths:
    - "D-01: schema and migrations under apps/api/prisma/; D-04: single schema.prisma; D-05: init migration via migrate dev --name init"
    - "D-02: root db:migrate, db:seed, db:studio, db:reset, db:generate delegate via pnpm --filter @kramnik/api exec prisma"
    - "D-03: apps/api postinstall runs prisma generate; D-06/D-07: seed path apps/api/prisma/seed.ts registered in package.json prisma.seed (seed body in plan 02-02); D-07 path documented for Phase 3 packages/types"
    - "D-08: Category enum HOME, GARBAGE, SCRAPS, GARDEN_GNOMES on Product"
    - "D-09: Product fields id, name, slug (unique), description, price, imageUrl, category, createdAt, updatedAt only — no stock, sku, isActive, deletedAt"
    - "D-10–D-11: imageUrl external placeholders in seed (02-02); all products visible without publish flags"
    - "D-12: Decimal @db.Decimal(10,2) on Product.price, OrderItem.unitPrice, Order.total"
    - "D-13: USD implicit — no currency column on Product or Order"
    - "D-14: OrderItem.unitPrice and Order.total snapshot purchase amounts"
    - "D-15: README or schema comment notes Phase 3 maps Decimal to string in JSON (no DTO work this phase)"
    - "D-16: Role enum CUSTOMER | ADMIN"
    - "D-17: User.passwordHash column present (bcrypt populated in 02-02 seed)"
    - "D-18: User has email (unique), passwordHash, role, timestamps — no name field"
    - "D-20: OrderStatus enum PENDING, CONFIRMED, SHIPPED, CANCELLED"
    - "D-21: Order.shippingAddress is a single String field"
    - "D-22: OrderItem includes productName snapshot plus productId, quantity, unitPrice"
    - "D-23–D-24: no orders in seed (02-02); OrderItem→Product onDelete Restrict"
    - "prisma and @prisma/client pinned to 6.19.3 (not Prisma 7)"
    - "pnpm db:migrate applies init migration to live shop_dev after schema complete"
    - "Vitest prisma-schema.test.ts passes on committed schema"
  artifacts:
    - path: apps/api/prisma/schema.prisma
      provides: DATA-01 entity definitions
      contains: "model User"
    - path: apps/api/prisma/migrations
      provides: DATA-02 migration history
      min_lines: 1
    - path: package.json
      provides: Root db:* orchestration
      contains: "db:migrate"
    - path: tests/data/prisma-schema.test.ts
      provides: Static schema contract tests
      contains: "passwordHash"
  key_links:
    - from: package.json
      to: apps/api/.env
      via: pnpm --filter @kramnik/api exec prisma
      pattern: "@kramnik/api exec prisma"
    - from: apps/api/prisma/schema.prisma
      to: DATABASE_URL
      via: env("DATABASE_URL")
      pattern: 'env\\("DATABASE_URL"\\)'
    - from: apps/api/package.json
      to: node_modules/@prisma/client
      via: postinstall prisma generate
      pattern: "prisma generate"
---

## Phase Goal

**As a** developer, **I want to** define the PostgreSQL schema in Prisma and apply the initial migration, **so that** the database is the single source of truth before seed data and API layers.

<objective>
Install Prisma 6.19.3 in `apps/api`, author the full `schema.prisma` per locked CONTEXT decisions, add root `db:*` scripts, Wave 0 schema Vitest checks, and apply the initial migration to `shop_dev`.

Purpose: DATA-01 and DATA-02 — schema contract and clean migrate on fresh DB.
Output: `apps/api/prisma/schema.prisma`, `migrations/*/init/`, generated client, root db scripts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/KRAM-02-prisma-data-model/02-CONTEXT.md
@.planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md
@.planning/phases/KRAM-02-prisma-data-model/02-PATTERNS.md
@.planning/phases/KRAM-02-prisma-data-model/02-VALIDATION.md
@package.json
@apps/api/package.json
@apps/api/.env.example
@docker-compose.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify Prisma stack packages before install</name>
  <files>.planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md</files>
  <read_first>.planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md — Package Legitimacy Audit</read_first>
  <action>Pause before any `pnpm add` for Prisma stack. Human reviews Package Legitimacy Audit table and npmjs.com pages for prisma@6.19.3, @prisma/client@6.19.3, bcryptjs@3.0.3, tsx@4.22.2. Confirm Prisma 7.x is not approved. Resume only after human types approved.</action>
  <what-built>Package list for Phase 2 apps/api: prisma@6.19.3, @prisma/client@6.19.3, bcryptjs@3.0.3, tsx@4.22.2 (slopcheck unavailable — all [ASSUMED])</what-built>
  <how-to-verify>
    1. Open npmjs.com: `prisma`, `@prisma/client`, `bcryptjs`, `tsx`.
    2. Confirm publishers/repos: github.com/prisma/prisma, github.com/dcodeIO/bcrypt.js, github.com/privatenumber/tsx.
    3. Confirm install versions are **6.19.3** for prisma packages — NOT `latest` (7.x breaks Nest CJS and seed-on-reset per RESEARCH).
    4. Reject typosquat names or missing source repos.
  </how-to-verify>
  <acceptance_criteria>
    - Human approves all four packages at pinned versions
    - Prisma 7.x explicitly rejected for this phase
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md</automated>
  </verify>
  <done>Human resume-signal approved received; Task 1 may install packages</done>
  <resume-signal>Type "approved" or list packages to exclude</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 schema tests, Prisma deps, and schema.prisma</name>
  <read_first>
    - .planning/phases/KRAM-02-prisma-data-model/02-VALIDATION.md — Wave 0 Requirements
    - .planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md — Pattern 1 (schema skeleton), Standard Stack
    - .planning/phases/KRAM-02-prisma-data-model/02-PATTERNS.md — apps/api/package.json, schema.prisma, root package.json
    - tests/foundation/postgres-scripts.test.ts — Vitest static read pattern
  </read_first>
  <files>
    tests/data/prisma-schema.test.ts
    apps/api/package.json
    apps/api/prisma/schema.prisma
    package.json
  </files>
  <behavior>
    - Test 1: schema file contains models User, Product, Order, OrderItem
    - Test 2: enums Role, Category, OrderStatus present
    - Test 3: field passwordHash on User; no plaintext password column
    - Test 4: Decimal fields use @db.Decimal(10, 2) on price/total/unitPrice
    - Test 5: OrderItem→Product relation includes onDelete: Restrict
    - Test 6: root package.json defines db:migrate, db:seed, db:studio, db:reset, db:generate
  </behavior>
  <action>
    Per D-01, D-04: create `apps/api/prisma/schema.prisma` as the only schema file. Generator `prisma-client-js`; datasource `postgresql` with `url = env("DATABASE_URL")`. Enums per D-08, D-16, D-20: `Category` (HOME, GARBAGE, SCRAPS, GARDEN_GNOMES), `Role` (CUSTOMER, ADMIN), `OrderStatus` (PENDING, CONFIRMED, SHIPPED, CANCELLED). Models per D-09, D-12–D-14, D-18, D-21–D-22: User (cuid id, unique email, passwordHash, role default CUSTOMER, timestamps); Product (slug unique, description, price Decimal(10,2), imageUrl, category, timestamps — no stock/sku/isActive/deletedAt per D-09); Order (userId, status default PENDING, shippingAddress, total Decimal(10,2), items relation, @@index userId); OrderItem (orderId, productId, productName snapshot, quantity Int, unitPrice Decimal(10,2)). Relations per D-24: Order→User onDelete Restrict; OrderItem→Order onDelete Cascade; OrderItem→Product onDelete Restrict. Pin **prisma@6.19.3** and **@prisma/client@6.19.3** exactly — never `latest`. Add devDependency **tsx@4.22.2** and **bcryptjs@3.0.3** (seed in 02-02; install now per RESEARCH). Per D-03: `"postinstall": "prisma generate"` in apps/api scripts. Per D-06: add `"prisma": { "seed": "tsx prisma/seed.ts" }` in apps/api package.json (seed file created in 02-02). Per D-02: extend root `package.json` scripts: `db:migrate` → `pnpm --filter @kramnik/api exec prisma migrate dev`, `db:seed` → `... db seed`, `db:studio`, `db:reset` → `... migrate reset`, `db:generate` → `... generate` — keep existing `db:up`/`db:down`. Do NOT add PrismaModule, Nest services, REST routes, or packages/types generate (Phase 3). Add brief comment in schema or apps/api README stub line for D-15 (Decimal→string in public JSON) and D-07 (Phase 3 types path `../../apps/api/prisma/schema.prisma`). Create `tests/data/prisma-schema.test.ts` using readFileSync pattern from postgres-scripts.test.ts; assert model/enum/field/relation strings above. Run `pnpm install` from root after package changes.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/data/prisma-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` matches all locked model/enum/relation decisions (D-08–D-24)
    - Root and api package.json contain db:* and postinstall/prisma.seed wiring (D-02, D-03, D-06)
    - Prisma versions are 6.19.3 in apps/api/package.json
    - `pnpm exec vitest run tests/data/prisma-schema.test.ts` passes
  </acceptance_criteria>
  <done>Schema authored; static DATA-01 tests green; CLI wiring ready for migrate</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply initial migration to live database</name>
  <read_first>
    - .planning/phases/KRAM-02-prisma-data-model/02-RESEARCH.md — Pattern 4 (fresh DB proof)
    - apps/api/.env.example
    - scripts/verify-postgres.sh — precondition pattern
  </read_first>
  <files>
    apps/api/prisma/migrations
    apps/api/.env
  </files>
  <action>
    **[BLOCKING — schema push required]** Per D-05 and DATA-02: with Docker running, execute from repo root: (1) `pnpm db:up`, (2) ensure `apps/api/.env` exists (copy from `.env.example` if missing — gitignored), (3) first-time init: `pnpm --filter @kramnik/api exec prisma migrate dev --name init` (creates `apps/api/prisma/migrations/*_init/migration.sql` — do not hand-write SQL). Commit migration folder. Run `pnpm db:generate` or rely on postinstall to refresh client. Verify with `pnpm --filter @kramnik/api exec prisma migrate status` — no pending migrations. If migrate fails (P1012, shadow DB), fix DATABASE_URL/cwd (must use filter so apps/api/.env loads per D-02). Do not run seed in this task (02-02). Do not reset production-like data; local shop_dev only.
  </action>
  <verify>
    <automated>pnpm --filter @kramnik/api exec prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/migrations` contains init migration SQL generated by Prisma CLI
    - `prisma migrate status` reports database schema up to date
    - Tables User, Product, Order, OrderItem exist in shop_dev (optional: docker compose exec psql \dt)
  </acceptance_criteria>
  <done>DATA-02 satisfied — migrations applied to live local DB</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → npm registry | prisma, @prisma/client, bcryptjs, tsx installs after Task 0 approval |
| Prisma CLI → PostgreSQL | DDL via migrate dev writes tables; DATABASE_URL from apps/api/.env |
| Schema file → git | passwordHash column defined; no secrets in schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (prisma 6.19.3, bcryptjs, tsx) | mitigate | blocking-human checkpoint Task 0; pin 6.19.3 not latest 7.x |
| T-02-01 | Information disclosure | password storage | mitigate | D-17: User.passwordHash field only; schema test asserts passwordHash, no plaintext password column |
| T-02-02 | Elevation of privilege | Role enum | mitigate | D-16: Role enum ADMIN \| CUSTOMER in schema |
| T-02-03 | Tampering | Order integrity | mitigate | D-24: onDelete Restrict on OrderItem.productId — products referenced by orders cannot be deleted |
| T-02-04 | Tampering | Prisma 7 accidental install | mitigate | Exact pin 6.19.3 in package.json; checkpoint rejects latest |
| T-02-05 | Information disclosure | DATABASE_URL in .env | mitigate | apps/api/.env gitignored (Phase 1); copy from .env.example only |
</threat_model>

<verification>
- `pnpm exec vitest run tests/data/prisma-schema.test.ts` passes
- `pnpm db:up` then `pnpm --filter @kramnik/api exec prisma migrate status` shows no pending migrations
- No Nest PrismaModule, no REST, no packages/types implementation added
</verification>

<success_criteria>
- DATA-01: schema defines User, Product, Order, OrderItem with enums and relations
- DATA-02: init migration applies cleanly on fresh shop_dev
</success_criteria>

<output>
Create `.planning/phases/KRAM-02-prisma-data-model/02-01-SUMMARY.md` when done
</output>
