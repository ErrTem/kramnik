---
phase: 01-monorepo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - turbo.json
  - tsconfig.base.json
  - tsconfig.json
  - eslint.config.js
  - .prettierrc
  - .gitignore
  - vitest.config.ts
  - tests/foundation/workspace-layout.test.ts
  - tests/foundation/turbo-config.test.ts
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/index.html
  - apps/web/tsconfig.json
  - apps/web/tsconfig.app.json
  - apps/web/tsconfig.node.json
  - apps/web/eslint.config.js
  - apps/web/src/main.tsx
  - apps/web/src/index.css
  - apps/web/src/app/AppShell.tsx
  - apps/web/src/shared/.gitkeep
  - packages/types/package.json
  - packages/types/tsconfig.json
  - packages/types/src/index.ts
autonomous: false
requirements:
  - FOUND-01
user_setup: []

must_haves:
  truths:
    - "D-01: pnpm workspaces sole package manager; D-02: no package-lock.json; D-03: Corepack packageManager pnpm@9.15.9; D-04: default pnpm layout"
    - "D-05: @kramnik/types stub; D-06: types tsc build; D-07: turbo dev depends on @kramnik/types#build"
    - "D-16: minimal Kramnik Shop shell; D-17: demo assets removed; D-18: src/app + src/shared; D-19: index.html title Kramnik Shop"
    - "D-20: root eslint extended by apps; D-21: tsconfig.base.json; D-22: Prettier format script"
    - "D-09: Postgres not in turbo dev (db scripts only at root until 01-03)"
    - "`apps/web`, `apps/api` (empty dir or placeholder), and `packages/types` exist under workspace"
    - "`pnpm --filter @kramnik/web dev` serves Kramnik Shop shell on port 5173 (D-10)"
    - "Vitest smoke tests pass for workspace layout and turbo.json dev dependency on @kramnik/types#build"
  artifacts:
    - path: pnpm-workspace.yaml
      provides: Workspace package globs
      contains: "apps/*"
    - path: turbo.json
      provides: Turbo task graph
      contains: "@kramnik/types#build"
    - path: apps/web/src/app/AppShell.tsx
      provides: Minimal shop shell (no Vite demo)
      contains: "Kramnik Shop"
    - path: packages/types/dist/index.js
      provides: Compiled types stub
      min_lines: 1
  key_links:
    - from: apps/web/index.html
      to: apps/web/src/main.tsx
      via: script type=module src=/src/main.tsx
      pattern: "src=\"/src/main.tsx\""
    - from: apps/web/src/main.tsx
      to: apps/web/src/app/AppShell.tsx
      via: import AppShell
      pattern: "AppShell"
    - from: turbo.json
      to: packages/types
      via: dev dependsOn @kramnik/types#build
      pattern: "@kramnik/types#build"
---

## Phase Goal

**As a** developer, **I want to** install workspace dependencies and run the web app from `apps/web`, **so that** the brownfield Vite stack lives in the monorepo layout before adding the API.

<objective>
Bootstrap pnpm + Turborepo workspace, `@kramnik/types` stub build, Wave 0 Vitest smoke tests, and migrate the root Vite React app into `apps/web` with a minimal Kramnik Shop shell.

Purpose: FOUND-01 layout and safe brownfield move (D-01–D-07, D-16–D-21, D-22).
Output: Runnable `@kramnik/web` via filter; root npm artifacts removed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/KRAM-01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md
@.planning/phases/KRAM-01-monorepo-foundation/01-PATTERNS.md
@.planning/phases/KRAM-01-monorepo-foundation/01-VALIDATION.md
@.planning/phases/KRAM-01-monorepo-foundation/01-SKELETON.md
@package.json
@vite.config.ts
@eslint.config.js
@tsconfig.json
@tsconfig.app.json
@src/App.tsx
@src/main.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify npm packages before root install</name>
  <read_first>.planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md — Package Legitimacy Audit</read_first>
  <what-built>Package list for Phase 1 root devDependencies (slopcheck unavailable per research)</what-built>
  <how-to-verify>
    1. Open each package on npmjs.com: `turbo`, `prettier`, `vitest`, `eslint-config-prettier` (if used).
    2. Confirm publisher, recent version, and GitHub repo match RESEARCH.md table (vercel/turborepo, prettier/prettier, vitest-dev/vitest).
    3. Reject any package with typosquat name or no source repo.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all four packages are legitimate npm packages
    - No [SLOP] packages approved for install
  </acceptance_criteria>
  <resume-signal>Type "approved" or list packages to exclude</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Wave 0 — Vitest scaffold and workspace root files</name>
  <read_first>
    - .planning/phases/KRAM-01-monorepo-foundation/01-VALIDATION.md — Wave 0 Requirements
    - .planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md — Pattern 1 (pnpm root)
    - package.json (brownfield scripts to retire)
    - .gitignore
  </read_first>
  <files>
    package.json
    pnpm-workspace.yaml
    tsconfig.base.json
    tsconfig.json
    .prettierrc
    .gitignore
    vitest.config.ts
    tests/foundation/workspace-layout.test.ts
    tests/foundation/turbo-config.test.ts
  </files>
  <action>
    Per D-01, D-03, D-04, D-21, D-22: enable Corepack and set root `package.json` to `name: "kramnik-shop"`, `private: true`, `packageManager: "pnpm@9.15.9"`, scripts `dev`/`build`/`lint`/`format`/`db:up`/`db:down` (format uses Prettier glob from RESEARCH Pattern 1; db scripts may call docker compose even before compose file exists in plan 01-03). Create `pnpm-workspace.yaml` with `apps/*` and `packages/*`. Delete `package-lock.json` per D-02. Hoist compiler defaults from `tsconfig.app.json` into `tsconfig.base.json`; root `tsconfig.json` uses project references pattern only. Add `.prettierrc` with sensible defaults (semi, singleQuote). Add `apps/api/.env` to `.gitignore` per RESEARCH security note. Create `vitest.config.ts` at repo root with `test.include: ['tests/**/*.test.ts']`. Add `tests/foundation/workspace-layout.test.ts` asserting directories `apps/web`, `apps/api`, `packages/types` exist (create empty `apps/api/.gitkeep` if needed for FOUND-01 path only — no Nest code yet). Add `tests/foundation/turbo-config.test.ts` that reads `turbo.json` and expects `dev.dependsOn` to include `@kramnik/types#build` (test may fail until Task 2 writes turbo.json — run after Task 2 or write stub turbo.json in Task 1). Install root devDependencies: `turbo`, `prettier`, `vitest`, `typescript` aligned ~6.0.2, `@types/node`. Do NOT run `npm install` at root. Run `corepack enable` and `pnpm install` to generate `pnpm-lock.yaml`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/workspace-layout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-lock.yaml` exists and `package-lock.json` does not
    - `pnpm-workspace.yaml` lists apps and packages globs
    - `package.json` contains `packageManager: "pnpm@9.15.9"`
    - `.gitignore` includes `apps/api/.env`
    - Workspace layout vitest passes
  </acceptance_criteria>
  <done>Root workspace installs with pnpm; Wave 0 layout test green</done>
</task>

<task type="auto">
  <name>Task 2: @kramnik/types stub and turbo.json pipelines</name>
  <read_first>
    - .planning/phases/KRAM-01-monorepo-foundation/01-RESEARCH.md — Patterns 2 and 5
    - .planning/phases/KRAM-01-monorepo-foundation/01-PATTERNS.md — packages/types section
  </read_first>
  <files>
    turbo.json
    packages/types/package.json
    packages/types/tsconfig.json
    packages/types/src/index.ts
    eslint.config.js
  </files>
  <action>
    Per D-05, D-06, D-07: create `packages/types` with `name: "@kramnik/types"`, `private: true`, `main`/`types`/`exports` pointing to `dist/`, scripts `build: tsc -p tsconfig.json` and `lint: eslint .`. `src/index.ts` exports empty object `export {}`. `tsconfig.json` extends `../../tsconfig.base.json` with `composite: true`, `declaration: true`, `outDir: dist`, `rootDir: src`, `noEmit: false`. Create `turbo.json` per RESEARCH Pattern 2: `build.dependsOn: ["^build"]`, `build.outputs: ["dist/**"]`, `dev.dependsOn: ["@kramnik/types#build"]`, `dev.cache: false`, `dev.persistent: true`, `lint.dependsOn: ["^build"]`. Never add `dependsOn: ["^dev"]`. Refactor root `eslint.config.js` to shared base: `globalIgnores(['dist', '**/node_modules'])` + `tseslint.configs.recommended` only (no React plugins at root) per D-20 and PATTERNS. Run `pnpm --filter @kramnik/types build` and `pnpm exec vitest run tests/foundation/turbo-config.test.ts`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/foundation/turbo-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/types/dist/index.js` and `index.d.ts` exist after build
    - `turbo.json` dev task depends on `@kramnik/types#build` exactly
    - Turbo config vitest passes
  </acceptance_criteria>
  <done>@kramnik/types builds; Turbo graph matches D-07</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Vite app to apps/web with minimal shell</name>
  <read_first>
    - .planning/phases/KRAM-01-monorepo-foundation/01-PATTERNS.md — apps/web sections
    - vite.config.ts, index.html, src/main.tsx, src/index.css, src/App.tsx
    - public/ (if present)
  </read_first>
  <files>
    apps/web/package.json
    apps/web/vite.config.ts
    apps/web/index.html
    apps/web/tsconfig.json
    apps/web/tsconfig.app.json
    apps/web/tsconfig.node.json
    apps/web/eslint.config.js
    apps/web/src/main.tsx
    apps/web/src/index.css
    apps/web/src/app/AppShell.tsx
    apps/web/src/shared/.gitkeep
  </files>
  <action>
    Per D-16–D-19: Move `index.html`, `vite.config.ts`, `src/`, `public/` into `apps/web/` unchanged except paths/title. Set `apps/web/package.json` `name: "@kramnik/web"` with brownfield dependencies and scripts (`dev`, `build`, `lint`, `preview`) from root package.json. Extend `tsconfig.base.json` in app tsconfigs; replicate root `tsconfig.json` references under `apps/web`. Create `apps/web/eslint.config.js` importing shared root rules + React hooks/refresh + `globals.browser` (migrate from current root eslint.config.js) with `parserOptions.project` relative to `import.meta.dirname`. Per D-16–D-17: delete demo UI — create `AppShell.tsx` with functional component rendering `&lt;main&gt;&lt;h1&gt;Kramnik Shop&lt;/h1&gt;&lt;/main&gt;` only; remove `App.tsx`, `App.css`, `src/assets/` demo files. Update `main.tsx` to import `AppShell` from `./app/AppShell.tsx`. Simplify `index.css` to `@import "tailwindcss";` plus minimal `body { margin: 0; }` (strip marketing layout rules). Set `index.html` title to `Kramnik Shop`. Add `src/shared/.gitkeep` per D-18. Verify `pnpm --filter @kramnik/web dev` serves shell before deleting root `src/`, `vite.config.ts`, `index.html`, `public/`, root tsconfig.app/node if duplicated. Remove root Vite-specific devDependencies from root package.json (keep only workspace orchestration deps). Do not implement Nest or Docker in this task.
  </action>
  <verify>
    <automated>pnpm --filter @kramnik/web build</automated>
  </verify>
  <acceptance_criteria>
    - No `src/` or `vite.config.ts` at repo root
    - `apps/web` builds without TypeScript errors
    - Built or dev output shows heading text Kramnik Shop (no Vite counter/hero)
    - `index.html` title is Kramnik Shop
  </acceptance_criteria>
  <done>Web app runs from apps/web; brownfield demo removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → npm registry | External packages installed per Task 0 approval |
| Developer → git | Lockfile and workspace config committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (turbo, prettier, vitest) | mitigate | blocking-human checkpoint Task 0; verify npmjs publisher |
| T-01-03 | Information disclosure | apps/api/.env | mitigate | add `apps/api/.env` to `.gitignore` in Task 1 |
| T-01-04 | Tampering | workspace packages | mitigate | `@kramnik/types` `private: true`; pnpm workspace protocol only |
</threat_model>

<verification>
- `pnpm exec vitest run` passes foundation tests
- `pnpm --filter @kramnik/web dev` reachable on 5173 (manual spot-check optional)
- No root `package-lock.json`
</verification>

<success_criteria>
- FOUND-01 directory layout and types stub build wired in turbo.json
- Developer can install and run web package from monorepo
</success_criteria>

<output>
Create `.planning/phases/KRAM-01-monorepo-foundation/01-01-SUMMARY.md` when done
</output>
